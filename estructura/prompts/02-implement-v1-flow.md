# Prompt — Implement V1 Flow

Read the full repertoire. Implement the complete v1 flow: lead intake -> strategy -> proposal -> ops -> qa.

Requirements:
- typed schemas
- one orchestrator route
- prompt files on disk
- persisted workflow state
- one integration-safe dry-run path
- tests for the happy path

Do not collapse the system into one giant prompt.
