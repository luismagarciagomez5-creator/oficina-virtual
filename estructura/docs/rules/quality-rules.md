# Quality Rules

- every workflow step needs a trace
- every agent needs a schema contract
- every proposal must expose assumptions
- every ops plan must be actionable
- every external action should be auditable
